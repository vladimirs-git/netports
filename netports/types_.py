"""Typing"""
from typing import (
    Any,
    Iterable,
    List,
    Optional,
    Set,
    Tuple,
)

IInt = Iterable[int]
IStr = Iterable[str]
LAny = List[Any]
LInt = List[int]
LStr = List[str]
OInt = Optional[int]
SInt = Set[int]

TStr5 = Tuple[str, str, str, str, str]
